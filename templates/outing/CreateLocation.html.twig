{% extends 'base.html.twig' %}
{% block title %}
    Home
{% endblock %}
{% block body %}
    <div class="container h-25">

    </div>
    <br>
    <div class=" container my-5 px-4 px-lg-5 d-flex h-100 align-items-center justify-content-center text-white">
        <h1> Créer une lieu </h1>
    </div>
    <br>

    <div class="container text-white">
    <div class="row">
    <div class="col">
        {{ form_start (formulaire) }}
        <div class="row">
            <div class="col">{{ form_row(formulaire.name) }}</div>
            <div class="col">{{ form_row(formulaire.address) }}</div>
        </div>
        <div class="row">
            <div class="col">{{ form_row(formulaire.city) }}</div>
            <div class="col">{{ form_row(formulaire.ajouter) }}</div>


            <div class="container mb-5">

                {{ form_end (formulaire) }}
            </div>
        </div>
    </div>

{% endblock %}